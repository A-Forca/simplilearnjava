import java.awt.*;

/*


    Challenge 2: Employee Class
    Create a class called Employee with the following attributes:

    name (String)
    age (int)
    salary (double)
    Include a constructor that initializes the name, age, and salary attributes.

            Also, include a method called getDetails() that returns a string representation of the employee's details in the format: "Name: [name], Age: [age], Salary: [salary]".

    In the main method, create an instance of the Employee class, provide values for the attributes, and print the employee's details using the getDetails() method.

    Challenge 3: Bank Account Class
    Create a class called BankAccount with the following attributes:

    accountNumber (String)
    balance (double)
    Include the following methods in the BankAccount class:

    deposit(double amount): Adds the specified amount to the account balance.
    withdraw(double amount): Subtracts the specified amount from the account balance.
    getBalance(): Returns the current account balance.
    In the main method, create an instance of the BankAccount class, perform some deposit and withdrawal operations, and print the account balance.

    Challenge 4: Book Class
    Create a class called Book with the following attributes:

    title (String)
    author (String)
    price (double)
    Include a constructor that initializes the title, author, and price attributes.

            Also, include a method called getBookInfo() that returns a string representation of the book's information in the format: "Title: [title], Author: [author], Price: [price]".

    In the main method, create an array of Book objects, initialize them with different book details, and print the information of each book using the getBookInfo() method.

    Challenge 5: Student Grade Calculator
    Create a class called Student with the following attributes:

    name (String)
    grades (array of integers)
    Include a constructor that initializes the name and grades attributes.

    Also, include a method called calculateAverage() that calculates and returns the average grade of the student.

    In the main method, create an instance of the Student class, provide values for the name and grades, and print the student's name and average grade.
    */
public class aiChallenges {


}
